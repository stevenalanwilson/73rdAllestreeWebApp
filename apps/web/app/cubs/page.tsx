import { getSectionConfig } from '@73rd/tokens'
import { SectionPage } from '@/components/SectionPage'

export default function CubsPage() {
  return <SectionPage config={getSectionConfig('cubs')} />
}
